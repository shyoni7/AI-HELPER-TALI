# PLAN.md — AI-HELPER-TALI

מפת דרכים לבנייה. השלבים מסומנים בסטטוס. שלב 0 הושלם בקומיט זה.

---

## שלב 0 — שלד (הושלם ✅)

מטרה: בסיס שאפשר לבנות עליו, עם פרטיות מובנית.

- [x] פרויקט Next.js + TypeScript (App Router), הגדרות פריסה ל‑Vercel.
- [x] מודל נתונים ב‑Postgres: `therapists`, `patients`, `appointments`, `waitlist`, `conversations` (`db/schema.sql`) + אינדקסים, טריגרים ל‑`updated_at`, ואינדקס ייחודי שמונע הזמנה כפולה.
- [x] שלד מנוע הסוכן: לקוח Anthropic, לולאת Tool Use ידנית (`src/agent/engine.ts`), system prompt עם כללי בטיחות ופרטיות.
- [x] הגדרת הכלים: `get_center_info`, `check_availability`, `book_appointment`, `cancel_appointment`, `join_waitlist`. פעולות רגישות מסומנות `sensitive` ודורשות אישור משתמש.
- [x] שכבות port עם stubs מטיפוס: Google Calendar, מייל, WhatsApp, ומחשבון רשימת ההמתנה — כדי ששלב 1 ימלא מימוש בלי לשנות קוראים.
- [x] ממשק צ'אט מינימלי + נקודת קצה `POST /api/chat` עם ולידציה, בדיקת origin וטיפול בשגיאות.
- [x] לוגר מודע-פרטיות (רידקציה של מייל/טלפון/סודות), ניהול env מאומת (zod).
- [x] README + PLAN.

---

## שלב 1 — תיאום פגישות אמיתי + מייל (הבא 🔜)

מטרה: סוף-עד-סוף אמיתי מול Google Calendar והתראות מייל.

- [ ] **Google Calendar אמיתי:** החלפת `StubGoogleCalendar` במימוש `googleapis`:
  - freebusy מול היומן המרכזי + יומני המטפלים לבדיקת זמינות אמיתית.
  - יצירת/מחיקת אירועים בקביעה/ביטול (סנכרון דו-כיווני).
  - חיבור Service Account עם הרשאות לכל היומנים.
- [ ] **watch channels:** רישום התראות push כדי לזהות ביטולים שנעשו ישירות ביומן, עם נקודת קצה `POST /api/calendar/webhook` שמסנכרנת ל‑DB ומפעילה את רשימת ההמתנה.
- [ ] **מייל אמיתי:** חיבור ספק טרנזקציוני (Resend/SendGrid/SES) ב‑`src/notifications/email.ts`.
- [ ] **התמדת שיחות:** שמירת תמלילים ל‑`conversations` + שיוך מטופל (`patient_id`).
- [ ] **זיהוי מטופל:** session token / טלפון-מייל כדי לשייך שיחה למטופל קיים.
- [ ] בדיקות יחידה ל‑`computeOpenSlots`, `scoreCandidate`, `slotFitsWindows`.

## שלב 2 — WhatsApp + שיפור רשימת המתנה

- [ ] **WhatsApp:** מימוש `src/notifications/whatsapp.ts` מול WhatsApp Business Cloud API; ערוץ נכנס (webhook) להמשך שיחה ב‑WhatsApp.
- [ ] **רשימת המתנה מתקדמת:** פנייה ל‑top-N עם חלון "first-come" / hold זמני לזמן שהתפנה, ותפוגת הצעות.
- [ ] **לוח ניהול לצוות:** צפייה/עריכה של פגישות, מטפלים ורשימת המתנה.

## שלב 3 — הקשחה ותפעול

- [ ] בקרת קצב (rate limiting) ו‑CAPTCHA קל לנקודת הצ'אט.
- [ ] תפקידים והרשאות לצוות (auth).
- [ ] טלמטריה: שימוש בכלים, אחוזי המרה, זמני תגובה.
- [ ] ניטור שגיאות (Sentry) ו‑health checks.

---

## פרטיות ואבטחת מידע בריאותי (§Privacy)

עקרונות שמיושמים כבר בשלב 0 וממשיכים לאורך כל השלבים:

1. **מינימיזציה:** אוספים רק את ההכרחי. אין אחסון מידע קליני בשדות חופשיים. `appointments.reason` הוא תווית קצרה ולא-קלינית בלבד.
2. **מקור אמת יחיד ל‑PII:** שם/מייל/טלפון רק ב‑`patients`. שאר הטבלאות מפנות ב‑id — מה שמאפשר אנונימיזציה/מחיקה (זכות להישכח) בעדכון שורה אחת (`patients.anonymized_at`).
3. **הסכמה מפורשת:** `consent_contact` + `consent_at` נרשמים לפני פנייה יזומה. רשימת ההמתנה דורשת הסכמה.
4. **לוגים נקיים:** הלוגר עושה רידקציה אוטומטית של מייל/טלפון/סודות; debug כבוי כברירת מחדל.
5. **סודות לא בקוד:** כל הסודות ב‑env (`.env` ב‑gitignore), כולל מפתחות Google. headers אבטחה ב‑`next.config.mjs`.
6. **גבולות הסוכן:** הסוכן אינו נותן ייעוץ קליני; מפנה למטפל אנושי, ובמצוקה — לעזרה דחופה.
7. **שמירה והתיישנות (לפיתוח בשלב 1):** `conversations.transcript` ו‑`conversations.purged_at` מאפשרים מחיקת תמלילים בלוח זמנים; להגדיר retention job.

### מטלות פרטיות פתוחות לשלבים הבאים
- [ ] הצפנה במנוחה לעמודות רגישות (pgcrypto / KMS) או DB מוצפן.
- [ ] Data retention job אוטומטי (purge תמלילים ישנים).
- [ ] תיעוד DPIA / מדיניות פרטיות מול הדרישות הרגולטוריות הרלוונטיות.
- [ ] נקודת קצה למימוש זכות עיון/מחיקה של מטופל.

---

## הערות ארכיטקטורה

- **למה לולאת כלים ידנית ולא ה‑tool runner של ה‑SDK:** חלק מהכלים בלתי-הפיכים ופונים החוצה (קביעה, ביטול, פנייה לאנשים). לולאה ידנית מאפשרת תיעוד/בקרה של כל קריאה ופותחת דלת ל‑human-in-the-loop בלי שכתוב.
- **port + stub:** Google Calendar, מייל ו‑WhatsApp מוגדרים מאחורי ממשק (`CalendarPort`, `sendEmail`, `sendWhatsApp`). הקוראים תלויים בממשק בלבד — מימוש שלב 1/2 לא נוגע בלוגיקת הסוכן.
- **בטיחות מקבילית:** אינדקס `uq_appointments_active_slot` מונע הזמנה כפולה ברמת ה‑DB; הכלי מחזיר שגיאת קונפליקט ידידותית.
