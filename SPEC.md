# SPEC.md — אפיון מערכת AI-HELPER-TALI

> מסמך אפיון חי. גרסה 0.1 (טיוטה לאישור). מתבסס על החלטות שסוכמו עם בעל המוצר.
> מה שכבר נבנה בשלב 0 (תשתית) מתואר ב‑README; כאן מתואר **מה בונים** ולמה.

---

## 1. סקירה ומטרות

מערכת ווב למרכז מטפלים הכוללת:
1. **אתר שיווקי** ציבורי — מציג את המרכז, הטיפולים והמטפלים, ומזמין לפעולה (קביעת פגישה / שיחה עם הסוכן).
2. **סוכן AI מוטמע** (Claude + Tool Use) שיודע: לענות על שאלות, ליזום בקשת פגישה, לשלוח תזכורות, ולנהל רשימת המתנה חכמה.
3. **יומן ותיאום** מול Google Calendar — יומן מרכזי + יומן לכל מטפל, בדיקת זמינות אמיתית.
4. **לוח ניהול** לצוות — מנהל מערכת, מנהל מרכז ומטפלים.

עקרון‑על: **פרטיות ואבטחת מידע בריאותי מההתחלה** (ראו §7).

---

## 2. תפקידים והרשאות (Roles)

| תפקיד | מי | התחברות | יכולות עיקריות |
|---|---|---|---|
| **לקוח (Client)** | מבקר/ת באתר | אנונימי; ללא חשבון | גלישה, שיחה עם הסוכן, מילוי טופס בקשת פגישה, הצטרפות לרשימת המתנה |
| **מטפל (Therapist)** | עובד/ת המרכז | טלפון + OTP | צפייה ביומן וברשימת המטופלים שלו, אישור/דחיית בקשות שמופנות אליו, ביטול פגישות |
| **מנהל מרכז (Center Manager)** | בעל/ת המרכז | טלפון + OTP | כל יכולות המטפל + הוספה/עריכה של מטפלים, צפייה בכלל הבקשות והפגישות, ניהול רשימת המתנה, הגדרות תוכן האתר |
| **מנהל מערכת (System Admin)** | טכני/על | טלפון + OTP | כל היכולות + הגדרות מערכת, ניהול משתמשי צוות, גישה לטלמטריה/לוגים |

**הערות:**
- מטפלים אינם נרשמים בעצמם — מנהל המרכז מזין אותם ידנית (מספר קבוע של עובדים).
- ההתחברות לצוות היא במספר טלפון + קוד חד‑פעמי (OTP ב‑SMS). אין סיסמאות.
- לקוחות נשארים אנונימיים עד למילוי טופס; הפרטים שנמסרים בטופס יוצרים/מעדכנים רשומת `patient`.

---

## 3. ישויות נתונים (Entities)

הרחבה של מודל שלב 0 (`db/schema.sql`). שינויים נדרשים לאפיון הזה:

### קיים (ייתכנו התאמות קלות)
- `therapists` — מטפלים. **תוספת:** קישור למשתמש צוות, שעות עבודה (working hours), תיאור פומבי + תמונה לאתר.
- `patients` — מקור אמת ל‑PII של לקוחות.
- `appointments` — פגישות מאושרות (מסונכרנות ליומן).
- `waitlist` — רשימת המתנה חכמה.
- `conversations` — תמלילי שיחות הסוכן.

### חדש
- **`staff_users`** — משתמשי צוות (מטפל/מנהל מרכז/מנהל מערכת).
  - `id`, `phone` (ייחודי), `role` (enum: `therapist`/`center_manager`/`system_admin`), `therapist_id` (nullable — קישור לרשומת מטפל אם רלוונטי), `active`, חותמות זמן.
- **`auth_otp`** — קודי OTP חד‑פעמיים (hash, תפוגה, ניסיונות) — לא נשמר קוד גלוי.
- **`booking_requests`** — בקשות פגישה מלקוחות, **לפני** אישור צוות.
  - `id`, `patient_id`, `therapist_id` (מבוקש/nullable), `preferred_times` (JSONB), `topic` (תווית לא‑קלינית קצרה), `status` (`pending`/`approved`/`declined`/`expired`), `handled_by` (staff_user), `appointment_id` (משתבץ לאחר אישור), חותמות זמן.
- **`reminders`** — תזכורות מתוזמנות לפגישות (ערוץ, מתי לשלוח, סטטוס שליחה).
- **`site_content`** *(אופציונלי לשלב מאוחר)* — תוכן ניתן‑לעריכה של עמודי האתר.

> הסכמה תעודכן ב‑`db/schema.sql` עם הירוקציה (migration) ייעודית לאחר אישור האפיון.

---

## 4. זרימות עיקריות (Flows)

### 4.1 בקשת פגישה (לקוח) → אישור צוות
1. לקוח משוחח עם הסוכן או לוחץ "קביעת פגישה".
2. הסוכן/הטופס אוסף: פרטי קשר, מטפל מבוקש (אם יש), נושא קצר, וחלונות זמן מועדפים. **הסוכן מציע רק זמנים שאומתו** מול `check_availability`.
3. נוצרת `booking_request` במצב `pending`. הלקוח מקבל הודעת "הבקשה התקבלה, ממתינה לאישור".
4. הצוות (המטפל הרלוונטי / מנהל) רואה את הבקשה בלוח הניהול ומאשר/דוחה.
5. באישור: נוצרת `appointment` + אירוע ב‑Google Calendar, נשלח אישור ללקוח, ומתוזמנת תזכורת.
6. בדחייה: נשלחת הודעה מנומסת, ואופציה להצטרף לרשימת המתנה.

### 4.2 התחברות צוות
1. הקלדת טלפון → שליחת OTP ב‑SMS.
2. הקלדת קוד → יצירת סשן מאומת לפי תפקיד.

### 4.3 הוספת מטפל (מנהל מרכז)
1. מנהל מזין שם, טלפון, התמחויות, שפות, שעות עבודה, תיאור לאתר ויומן Google.
2. נוצרות רשומות `therapist` + `staff_user(role=therapist)`.

### 4.4 ביטול → רשימת המתנה חכמה
1. ביטול פגישה (ע"י לקוח דרך הסוכן, או ע"י צוות, או זוהה דרך watch ביומן).
2. הזמן מתפנה → המתאם (`waitlist/matcher`) מדרג מועמדים ושולח הצעה יזומה (מייל בשלב 1).

### 4.5 תזכורות
- לכל פגישה מאושרת מתוזמנת תזכורת (למשל 24 שעות לפני). שליחה במייל (שלב 1), WhatsApp (שלב 2).

---

## 5. מבנה האתר (IA)

**ציבורי:**
- בית (הירו + הזמנה לפעולה)
- אודות המרכז
- טיפולים / שירותים
- מטפלים (פרופילים)
- צור קשר / קביעת פגישה
- ווידג'ט צ'אט‑סוכן בכל העמודים

**אזור צוות (מאחורי התחברות):**
- לוח בקרה (בקשות ממתינות, פגישות היום)
- יומן
- בקשות פגישה (אישור/דחייה)
- מטופלים
- רשימת המתנה
- ניהול מטפלים *(מנהל מרכז+)*
- הגדרות *(מנהל מערכת)*

---

## 6. הסוכן (Agent)

- **מנוע:** Claude `claude-opus-4-8`, Tool Use, לולאה ידנית (כבר קיים).
- **כלים נוכחיים:** `get_center_info`, `check_availability`, `book_appointment`, `cancel_appointment`, `join_waitlist`.
- **התאמות לאפיון:** `book_appointment` ייצור **בקשה** (`booking_request`) ולא פגישה מאושרת — בהתאם לזרימת "בקשה → אישור". יתווסף כלי/לוגיקה לתזכורות.
- **גבולות:** אינו נותן ייעוץ קליני; פעולות רגישות מאושרות מול המשתמש; הפניה לעזרה דחופה במצוקה.

---

## 7. אבטחה ופרטיות

- מינימיזציה של מידע; PII מבודד ל‑`patients`; אין מידע קליני בשדות חופשיים.
- הסכמה מפורשת (`consent_contact`) לפני פנייה יזומה.
- אימות צוות ב‑OTP, סשנים מאובטחים, הרשאות לפי תפקיד (RBAC).
- לוגים עם רידקציה; סודות ב‑env בלבד; headers אבטחה.
- שמירה והתיישנות של תמלילים; מימוש זכות עיון/מחיקה. (פירוט ב‑PLAN §פרטיות.)

---

## 8. ארכיטקטורה טכנית

- **Frontend + Backend:** Next.js (App Router) על Vercel; עמודים ציבוריים (SSG/SSR) + אזור צוות מאומת + API routes.
- **DB:** Postgres (Vercel Postgres / Neon).
- **יומן:** Google Calendar API (freebusy + events + watch) דרך Service Account.
- **התראות:** ספק מייל טרנזקציוני (שלב 1) → WhatsApp Business (שלב 2); SMS ל‑OTP.
- **תזכורות/תזמון:** Vercel Cron (או תור) להרצת שליחת תזכורות ובדיקת רשימת המתנה.

---

## 9. תכנית עבודה — אבני דרך (Milestones)

> מעדכן את `PLAN.md`. שלב 0 (תשתית) הושלם.

- **M1 — אתר שיווקי בסיסי:** עמודים ציבוריים, פרופילי מטפלים מ‑DB, ווידג'ט הסוכן (מענה לשאלות בלבד). פריסה ל‑Vercel.
- **M2 — צוות + הזדהות:** `staff_users`, OTP ב‑SMS, RBAC, שלד לוח ניהול.
- **M3 — בקשות פגישה → אישור:** טופס/זרימת סוכן ליצירת `booking_request`, מסך אישור לצוות, יצירת `appointment` + אירוע יומן + אישור ללקוח.
- **M4 — יומן אמיתי + תזכורות:** מימוש Google Calendar (freebusy/events), watch לביטולים, תזכורות במייל דרך Cron.
- **M5 — רשימת המתנה חכמה:** הפעלת `matcher` בביטול, פנייה יזומה במייל.
- **M6 — WhatsApp + הקשחה:** ערוץ WhatsApp, rate limiting, טלמטריה, ניטור.

---

## 10. הנחות ושאלות פתוחות

1. **OTP ב‑SMS** מניח ספק SMS (למשל Twilio/Vonage). לאשר ספק מועדף.
2. **מנהל מערכת מול מנהל מרכז** — הנחתי ששניהם תפקידים נפרדים; אם זה אותו אדם בפועל, אפשר לאחד.
3. **שיוך בקשה למטפל** — האם לקוח חייב לבחור מטפל, או שאפשר "כל מטפל מתאים" והצוות משבץ? (הנחה: שניהם נתמכים.)
4. **תוכן האתר** — נערך בקוד תחילה; `site_content` לעריכה דרך ממשק יתווסף בשלב מאוחר.
5. **שפות** — עברית בלבד ל‑MVP? (הנחה: כן, עם תשתית להרחבה.)
